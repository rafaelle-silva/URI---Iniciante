contagem_positivos = 0
soma = 0.0
for i in range(1,7):
    number = float(input())
    if(number > 0):
        soma = soma + number
        contagem_positivos += 1

media = soma / contagem_positivos
print(f"{contagem_positivos} valores positivos")
print(f"{media:0.1f}")
